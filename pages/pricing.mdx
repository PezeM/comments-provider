import { DocsLayout } from '@/components/DocsLayout';
export default (props) => <DocsLayout {...props} />;

# Pricing

Comments Provider is a demo application and subscription and any payments are
currently disabled.
